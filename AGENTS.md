# 에이전트 / 신규 기여자 온보딩

이 저장소에서 실험을 이어가려는 사람 또는 AI 에이전트를 위한 규칙입니다.
이 규칙들은 전부 이 프로젝트에서 **비싸게 배운 것**이고, 위반 사례와 손실이 log.md 에 기록돼 있습니다.

## 읽는 순서

1. `README.md` — 전체 그림과 닫힌 축
2. `experiments/log.md` 머리말(인수인계 블록) — 확정 파이프라인·핵심 발견
3. 실험하려는 주제와 겹치는 Phase 절 — **같은 아이디어가 이미 기각됐는지 반드시 확인**
4. 필요 시 `docs/` 브리핑 1~3편 — 개념 해설 (창·origin·SMAPE·이론 하한 등)

## 실험 규칙 (요약 — 전체 23개는 log.md)

1. **분해능을 존중하라.** 내부 검증의 2σ ≈ 0.003. 이보다 작은 개선은 판정 불가.
   반드시 **짝시드**(A/B 를 같은 시드 집합으로) + 4폴드로 비교하라. 폴드당 잡음이 ±0.004 다
   (아무것도 안 바꿔도 그만큼 흔들린 실측 기록이 있음 — Phase 11-b R1).
2. **사전등록.** 후보·판정 기준·예상치를 실행 **전에** 적고, 사후에 바꾸지 않는다.
   설정 N개를 훑고 최댓값을 고르면 실력 차 없이도 ~1.6σ 가 공짜로 나온다.
3. **모양으로 판정하라.** 최적값이 고립된 봉우리면 노이즈. 진짜 효과는 넓은 고원.
   개선이 한 폴드(한 계절)에 몰리면 불신 — 실전 전이 실패가 2회 기록돼 있다.
4. **gain 중요도로 피처를 고르지 마라.** 이 프로젝트에서 gain 은 가치와 **역상관**으로 3회
   확인됐다 (item_id·name_cluster·r_dow — 암기 통로일수록 gain 이 높다).
5. **내부 검증이 못 보는 것 두 가지를 기억하라.**
   (a) 검증 연도 = 학습 연도 → **연도 드리프트 강건성은 내부에서 잴 수 없다**
   (b) 개입이 창 구성(origin 이 어디를 밟나)에 의존하면 내부 폴드는 실제 테스트의
       10개 고정 창을 대표하지 못한다 (규칙 ⑮ — 내부 4/4 통과 후 실전 실패 사례 있음)
6. **후처리 비교는 노이즈 0** (같은 .npy 에 함수만 교체) — 문턱 없이 판정 가능.
   재학습 동반 비교만 2σ̂ 문턱을 적용한다.
7. `features.py` 의 피처를 쓸 때는 **반드시 `F.active_columns()` / `F.active_names()`** 를 사용.
   직접 거르면 기각된 그룹이 조용히 재유입된다 (실제 사고 2회).
   검증 중인 새 그룹은 `DROPPED` 에 넣어 **기본 OFF** 로 두고 실험 스크립트에서만
   `F.active_columns(include=("내그룹",))` 로 켠다 — 안 그러면 기존 스크립트가 전부 조용히 바뀐다.
8. LightGBM `Dataset` 은 설정마다 새로 만들 것 (`feature_pre_filter` 함정).
9. 🚨 **양자화 후처리 뒤에서 개입을 평가하지 마라 (규칙 ⑲).** 정수 스냅은 작은 섭동을
   이산 점프로 증폭하고, 그 점프의 부호는 그 폴드에서만 유리하다. 실측 2건에서
   `seg+스냅` 은 양수인데 `하한만`(raw) 은 음수였다. **두 값이 어긋나면 raw 를 믿는다.**
   패턴은 `run_phase23b_routing_fair.py` 의 `post(seg=False, snap=False)` 참조.
10. **자유도를 늘리는 방향은 이 프로젝트에서 한 번도 이긴 적이 없다 (규칙 ⑳).**
    라우팅/조건부 결합은 자유도 193개가 자유도 1개한테 졌다. 유일하게 전이율 1.0 을 기록한
    부류는 **손실함수 대수에서 자유 상수 0개로 유도한 것**(정수 격자 스냅)이다.
11. **채택 판단은 전제가 아니라 이득의 분포로 (규칙 ㉒).** 전제(σ비 등)가 3회 재현돼도
    점수 이득이 창 하나에 90% 몰려 있으면 그건 고립 봉우리다.
12. **다른 모델 클래스의 피처를 이식하기 전 (규칙 ㉓)**: 그게 **없던 정보**인지
    **그 모델이 만들 수 없던 인코딩**인지 가른다. 트리는 단조변환에 불변이라
    기존 피처의 재인코딩은 희석 비용만 남긴다.

## 파이프라인 실행

```bash
# 1) 데이터 배치 (README 참조) 후 원본 예측 생성 (~10분, 시드 5)
python src/run_phase6a_calibration.py        # → experiments/phase6a_test_raw.npy

# 2) 확정 후처리로 제출본 생성 (재학습 0초)
python src/make_submission.py <이름> --seg 0.55,0.90,1.02 --t 1.8,10 --snap geom

# 실험용 채점 도구: run_phase10c_thresholds.py 의 cell_weights() 가
# 공식 지표를 칸별 가중합으로 분해해 준다 (수만 번 재채점 가능)
```

## 지금 열려 있는 실험

**대회 트랙(Tier A)에서 합법으로 남은 것은 사실상 두 개뿐이다.** 피처 축(사다리 2탄)은
Phase 19~20 에서 v25 실전 확인까지 하고 닫혔고, 앙상블·조건부결합 축은 Phase 21~29 에서
5가지 형태 전부 닫혔다. 재시도 전에 README 의 "닫힌 축" 표를 읽을 것.

1. **전환 타이밍을 창에서 읽는 피처** — `config.py` 의 화담숲 개장일이 3/20 인데 **실측 3/29~31**.
   ⚠️ **달력 상수 기반은 이미 실패했다**(Phase 29, 규칙 ㉓ — 트리는 `month`·`day` 로
   이미 그걸 만들 수 있어서 희석 비용만 남았다). 남은 것은 **창 28일 안에서 개장을 감지**하는
   설계다. 그건 (월,일)의 함수가 아니므로 규칙 ㉓ 에 걸리지 않는다.
2. **드리프트 강건화 변형** — E2(창 상대 타깃) 기각 원인은 "전환기 램프는 창에 없는 정보"였다.
   절대/상대 혼합 타깃, 앵커 안정화가 미시도.

## 🔀 외부 데이터 트랙 (Tier B) — 규칙이 달라진다

후속 작업은 **외부 API 를 쓰는 방향**으로 이어진다. 그건 **대회 규정 밖**이므로:

- 결과에 **`Tier B (외부데이터)`** 라벨을 붙이고 `v24` 계열 번호를 재사용하지 않는다 (`x1`, `x2`, …)
- Tier A(합법) 성적표와 **같은 표에 섞지 않는다.** 섞는 순간 "우리 최종 성적"을 아무도 답할 수 없다
- 평가 Input/Target 학습 사용·pseudo-labeling 은 Tier B 에서도 금지 — 규칙이 아니라 측정의 문제다

**무엇을 겨냥할지는 README 의 "인수인계" 절에 측정치와 함께 정리해 뒀다.**
요약: ① 드리프트 보정(예약률·투숙객수) ② 봄(유일하게 이론 하한과 거리가 남음)
③ 야외 업장 날씨(화담숲·느티나무 — 단 업장군을 나눠서. 전 업장 공통 피처는 Phase 9-b 처럼 기각됨).

**저울은 그대로 쓴다.** 데이터가 늘어도 짝시드·4폴드·사전등록·raw 확인은 그대로다.
오히려 외부 데이터는 자유도를 크게 늘리므로 규칙 ⑦(고립 봉우리)·⑳(자유도 비용)이 더 중요해진다.

## 하지 말 것

- README 의 "닫힌 축" 표에 있는 것을 기각 사유를 읽지 않고 재시도하는 것.
- Tier A 실험에서 외부 데이터·크롤링·API 를 쓰는 것 (공휴일·요일 등 달력 도메인 지식만 허용).
- **채점 결과로 알아낸 것을 규칙으로 만드는 것.** Public/Private 분할을 알아냈다고 해서
  파일별로 다르게 처리하면 그건 성적이 아니라 시험지 맞춤이다 (Phase 14 에서 폐기한 프로그램).
