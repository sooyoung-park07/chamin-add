# 곤지암리조트 식음업장 수요예측 — LG Aimers 7기 (DACON 236559)

리조트 내 9개 식음업장 · 193개 메뉴의 일별 판매량을 28일 창(window)만 보고 다음 7일 예측하는 대회.
테스트는 2024-06 ~ 2025-05 **1년**을 덮는 10개 구간, 지표는 3중 평균 가중 SMAPE (낮을수록 좋음).

| | Private (전체 테스트) |
|---|---|
| 첫 제출 (v1, 기본 LightGBM) | 0.4607 |
| **최종 (v17)** | **0.4391** |
| 대회 1위 | 0.437 |

> **이 프로젝트의 한 줄 요약: 개선의 83%가 모델이 아니라 "평가식이 무엇을 벌하고 무엇을 봐주는지"의
> 이해에서 나왔다.** 모델 튜닝(설정 79개 탐색)의 순수익은 17%였다.

---

## 점수를 만든 세 가지 (전부 후처리)

```
v1   0.4607  ┃ 기준선: 글로벌 LightGBM · 피처 57 · 시드 5 평균
             ┃
v8   0.4570  ┃ ← 하이퍼파라미터 79개 탐색 + 앙상블 7건        +0.0037 (17%)
             ┃
v9   0.4439  ┃ ← ① 3구간 후보정                             +0.0131 (61%)
             ┃
v12  0.4412  ┃ ← ② 정수 격자 스냅                            +0.0026 (12%)
             ┃
v17  0.4391  ┃ ← ③ 클램프 임계 2.0 → 1.8                     +0.0021 (10%)
```

**① 3구간 후보정** — 예측을 크기 구간별로 진단하니 **작은 값은 과대, 큰 값은 과소**로 안쪽 압축돼
있었다. `p<2 → ×0.55 · 2~10 → ×0.90 · ≥10 → ×1.02` 후 하한 1.0. OOF에서 적합, 자유도 3.

**② 정수 격자 스냅** — SMAPE 항은 항등식 `2|A−P|/(A+P) = 2·tanh(|ln(A/P)|/2)` 로 **로그비율에
대칭**이다. 실측이 항상 정수이므로 두 정수 사이 예측은 항상 손해 → 전부 정수로 사영한다.
경계는 산술중점이 아니라 **기하중점 `√(k(k+1))`** (1↔2 는 1.414). **맞출 파라미터가 0개**인 개선.

**③ 임계 재조정** — ②가 1구간 배율을 무력화(`c1<0.7071`이면 전부 동일 함수)하면서 진짜 레버가
"어디까지 1로 찍을 것인가"(t1)로 드러남. 2.0 → 1.8 (내부 고원 [1.70, 1.90]의 중앙).

핵심 전제: **실측 0인 칸은 채점 제외** → 문제는 "팔릴까"가 아니라 "팔린다면 몇 개일까"
→ 학습에서 y=0 행 제거 (이것만으로 0.895 → 0.543).

---

## 저장소 구조

```
src/
  config.py dataio.py features.py metrics.py validate.py model_lgbm.py   ← 파이프라인 코어
  make_submission.py    ← 저장된 원본예측(.npy)에 후처리만 입혀 제출본 생성 (재학습 0초)
  make_ensemble.py      ← 제출 CSV들 → 등가중 평균 → .npy (매핑 검증 내장)
  run_phase*.py         ← 실험 스크립트 전부 (번호 = experiments/log.md 의 Phase)
experiments/
  log.md                ← ⭐ 실험 전체 이력. 무엇을 왜 했고 왜 기각됐는지 전부. 여기부터 읽을 것
  *.json                ← 각 실험의 수치 결과
docs/
  팀브리핑 1~3편 (PDF)   ← 비전공 팀원 눈높이의 해설 문서
AGENTS.md               ← AI 에이전트/신규 기여자 온보딩 (실험 규칙 포함)
```

## 재현 방법

1. [DACON 236559](https://dacon.io/competitions/official/236559/) 에서 데이터를 받아 아래처럼 배치
   (대회 규정상 데이터는 저장소에 포함하지 않음):
   ```
   data/train/train.csv · data/test/TEST_00.csv ~ TEST_09.csv · data/sample_submission.csv
   ```
2. 환경: `pip install -r requirements.txt` (Python 3.11)
3. 원본 예측 생성 (학습 ~10분): `python src/run_phase6a_calibration.py`
   → `experiments/phase6a_test_raw.npy`
4. 최종 제출본: `python src/make_submission.py final --seg 0.55,0.90,1.02 --t 1.8,10 --snap geom`

---

## 검증 체계 — 이 프로젝트에서 제일 비쌌던 부분

내부 CV가 한때 실제 순위를 **ρ = −0.90 (거꾸로)** 예측했다. 원인: 내부 검증은 학습 직후 0~3개월을
재는데 실제 채점은 0~11개월 뒤이고, **용량이 큰 모델일수록 학습기간에 맞춰 외운 것이 멀리서 무너진다**
(잎 수와 열화폭의 상관 +0.982). 데이터가 1.5년뿐이라 '거리'와 '학습량'은 분리 불가.

확정된 규약:
- **폴드 4개** (겨울/봄 × 근거리/원거리 — 검증 타깃 기준 실질 독립 2구간) 각각 채점 → 평균, 균등 가중
- **분해능 2σ ≈ 0.003** — 이보다 작은 차이는 판정 불가. 반드시 **짝시드**(같은 시드 집합으로 A/B) 사용
- **사전등록** — 후보와 판정 기준을 실험 전에 log.md 에 기록, 사후 변경 금지
- 최적값이 **고립된 봉우리면 노이즈** (진짜 효과는 넓은 고원), **한 폴드 편중 개선은 불신**
- 방법론 규칙 15개 전체는 `experiments/log.md` 참조

## 닫힌 축 — 재시도 전에 반드시 기각 사유를 읽을 것

| 축 | 결과 | log.md 위치 |
|---|---|---|
| 하이퍼파라미터 (79개 설정) | 순수익 0. 용량 확대는 실전에서 명확히 손해 | Phase 5-b/c/e/h |
| 알고리즘 앙상블 (LGBM+XGB+Cat) | 최고 단일 대비 +0.0005 이하 | Phase 5-a/d, 10-a |
| 모델 구조 분리 (horizon·세그먼트) | horizon 7/7 패배 | Phase 7-a |
| 학습 가중치를 지표에 정렬 | +0.0019, 일관성 없음 | Phase 7-b |
| 리조트 전체/이름 그룹/요일 프로파일 피처 | 전부 기각 | Phase 4-e, 9-b/c/d/e |
| 후처리 추가 최적화 (임계 벡터 13개) | 낙관 +0.001 / 교차검증 **−0.006** | Phase 10-c |
| 급등 창 부스트 | **내부 4/4 통과 후 실전 실패** — 규칙 ⑮의 탄생 | Phase 12 |
| 창 상대 타깃 (E2) | 내부 0/4 (−0.017) — 아래 "핵심 발견" 참조 | Phase 16 |

## 핵심 발견 (점수 밖의 성과)

1. **테스트 연도가 학습 연도보다 약했다.** 각 TEST 창을 학습연도 같은 달력 기간과 비교(합법)하면
   YoY 0.75~0.92 (스키 성수기 0.76, 11월만 1.29). Phase 15.
2. **모델의 창 탄력성은 0.31~0.49.** 창을 인위로 30% 약화시킨 반사실 실험에서 예측은 그 절반도 안
   따라온다 — 나머지는 학습연도에서 외운 절대 수준. Phase 15.
3. **그런데 그 암기는 결함이 아니라 트레이드오프다.** 창이 예고할 수 없는 전환기(스키 개막 램프)를
   달력 기억이 메꾼다. 암기를 제거한 E2는 겨울에서 −0.019~−0.047 로 참패했다. Phase 16.
4. **이론 하한**: 같은 품목·요일·연속 5주의 실측 흔들림(CV 중앙값 52%)만으로 계산한 하한이
   겨울 0.427~0.450 — **우리 겨울 성적 0.435는 이미 그 안**이다. 봄에만 0.03~0.05 여지. Phase 4장(브리핑 3편).

## 열린 문제 — 여기서 이어가면 됩니다

- **피처 선별 (미완)**: 그림자 대조(Boruta식) → 전 피처 오라클 스윕 → 짝시드 사다리.
  중복 창 통계 3개 제거(R5)가 내부 +0.0023·4/4 로 유일하게 통과한 상태 (Phase 11-b). 확인 제출 미실시.
- **드리프트 강건화**: E2(창 상대 타깃)는 기각됐지만 변형이 남아 있다 — 앵커를 더 안정적으로,
  달력 피처를 제약해 탄력성을 올리는 방법, 절대/상대 혼합 타깃 등.
- **봄 전환기**: `config.py` 의 화담숲 개장일이 3/20 인데 실측은 3/29~31. 전환 타이밍을 창에서
  읽어내는 피처는 미해결.
- 브리핑 3편의 Q1~Q3 (날씨 대체 신호 · 봄이 하한에서 먼 이유 · 업장 가중 구조 활용).

## 환경

Python 3.11 · lightgbm 4.6 · xgboost 3.2 · catboost 1.2.10 · pandas 3.0 · scikit-learn 1.8 · numpy

---

*실험 이력의 수치는 전부 `experiments/log.md` 와 `experiments/*.json` 에 있습니다.
새로 실험을 시작하기 전에 `AGENTS.md` 를 읽어 주세요 — 사람에게도 유효한 규칙입니다.*
