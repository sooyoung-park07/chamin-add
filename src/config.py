# -*- coding: utf-8 -*-
"""프로젝트 공통 설정 — 경로·상수·도메인 지식.

모든 모듈은 여기서만 경로/상수를 가져온다. (하드코딩 금지)
"""
import os

# ---------------------------------------------------------------- 경로
SRC_DIR = os.path.dirname(os.path.abspath(__file__))
ROOT = os.path.dirname(SRC_DIR)                       # 프로젝트 루트
DATA = os.path.join(ROOT, "data")
TRAIN_CSV = os.path.join(DATA, "train", "train.csv")
TEST_DIR = os.path.join(DATA, "test")
SUBMISSION_TPL = os.path.join(DATA, "sample_submission.csv")
LABELS_JSON = os.path.join(DATA, "labels", "menu_labels.json")
EXPERIMENTS = os.path.join(ROOT, "experiments")
SUBMISSIONS = os.path.join(ROOT, "submissions")

# ---------------------------------------------------------------- 대회 구조
WINDOW = 28          # 테스트마다 주어지는 관측 창(일)
HORIZON = 7          # 예측 대상 +1 ~ +7일
N_TEST = 10          # TEST_00 ~ TEST_09
PASS_PUBLIC = 0.711046   # LG Aimers 통과선 (Public)
PASS_PRIVATE = 0.693935

# 평가: 영업장 가중 SMAPE(0~2 스케일), 실측 0인 행은 채점 제외.
# 담하·미라시아 가중치가 높다(값 비공개) → 검증 시 민감도 확인용 후보들.
HIGH_WEIGHT_STORES = ("담하", "미라시아")
WEIGHT_CANDIDATES = (1.5, 2.0, 3.0)

# ---------------------------------------------------------------- 도메인 지식 (웹리서치 + 데이터 검증 완료)
# 군집: 성수기 방향이 다른 5그룹
STORE_CLUSTER = {
    "화담숲주막": "hwadam",      # 화담숲 안. 12~2월 완전 휴점, 10월 단풍 피크
    "화담숲카페": "hwadam",
    "포레스트릿": "ski",         # 슬로프 옆 스낵. 1월 피크
    "카페테리아": "ski",         # 스키하우스 푸드코트. 1월 피크
    "느티나무 셀프BBQ": "green",  # 야외 BBQ. 6월 피크 (겨울에도 영업은 함)
    "담하": "always",           # 빌리지 한식당. 연중
    "라그로타": "always",        # 이탈리안. 연중, 월요일 휴무
    "미라시아": "always",        # 뷔페·호텔조식. 연중
    "연회장": "b2b",            # 컨벤션. 목요일 피크, 일요일 최저
}

# 시즌 플래그 기준일 (근거: 곤지암 공식/보도자료)
HWADAM_OPEN = ((3, 20), (11, 30))     # 화담숲 개장기 (월,일)~(월,일)
SKI_SEASON_MONTHS = (12, 1, 2)        # +3월 5일까지
SKI_PEAK = ((12, 20), (1, 31))        # 얼리모닝 확대 구간
FOLIAGE = ((10, 15), (11, 20))        # 단풍 시즌

# ---------------------------------------------------------------- 전처리 확정값 (Phase 2)
# 가격은 원본 숫자보다 '등급'이 낫다 (CV 0.5171 → 0.5159). 빼면 오히려 나쁨(0.5200).
PRICE_BANDS = (3000, 8000, 20000, 50000)      # 5등급 경계
PRICE_BAND_NAMES = ("저가(~3천)", "중저가(~8천)", "중가(~2만)", "고가(~5만)", "초고가(5만~)")
# 스파이크 캡핑은 기각(SMAPE가 비율오차라 손해). 음수는 14개뿐(0.014%)이라 무시.

# 한국 공휴일 (2023~2025, 대체공휴일 포함)
HOLIDAYS = [
    "2023-01-01", "2023-01-21", "2023-01-22", "2023-01-23", "2023-01-24",
    "2023-03-01", "2023-05-05", "2023-05-27", "2023-05-29", "2023-06-06",
    "2023-08-15", "2023-09-28", "2023-09-29", "2023-09-30", "2023-10-02",
    "2023-10-03", "2023-10-09", "2023-12-25",
    "2024-01-01", "2024-02-09", "2024-02-10", "2024-02-11", "2024-02-12",
    "2024-03-01", "2024-04-10", "2024-05-05", "2024-05-06", "2024-05-15",
    "2024-06-06", "2024-08-15", "2024-09-16", "2024-09-17", "2024-09-18",
    "2024-10-01", "2024-10-03", "2024-10-09", "2024-12-25",
    "2025-01-01", "2025-01-27", "2025-01-28", "2025-01-29", "2025-01-30",
    "2025-03-01", "2025-03-03", "2025-05-05", "2025-05-06", "2025-06-03",
    "2025-06-06", "2025-08-15", "2025-10-03", "2025-10-05", "2025-10-06",
    "2025-10-07", "2025-10-08", "2025-10-09", "2025-12-25",
]

# ---------------------------------------------------------------- 데이터 무결성 기준값 (check_data.py가 대조)
EXPECTED = dict(
    train_rows=102_676,
    n_items=193,
    n_stores=9,
    train_start="2023-01-01",
    train_end="2024-06-15",
    submission_shape=(70, 194),
)
